import styled from "styled-components";
import tw from "twin.macro";

export const SProduct = styled.div`
  ${tw`flex flex-col w-full`}
`;

export const SProductInfo = styled.div`
  ${tw`w-full my-20 px-[120px]`}
`;
